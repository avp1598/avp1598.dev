import React from "react";

type Props = {};

const VSCode = (props: Props) => {
  return (
    <iframe
      src="https://github1s.com/spect-ai/circles.v1"
      title="VsCode"
      className="w-full border-none h-full"
    ></iframe>
  );
};

export default VSCode;
